services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: apu
    ports:
      - "5432:5432"
    volumes:
      - apu_postgres_data:/var/lib/postgresql/data

volumes:
  apu_postgres_data:
